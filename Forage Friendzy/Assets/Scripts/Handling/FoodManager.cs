using System.Collections;
using UnityEngine;

//Spawner -> list of players -> search for prey components on those

//FoodManager -> array of ints -> indexed based on localClientID -> on pickup or etc, change your value

public class FoodManager : MonoBehaviour
{

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}